syntax = "proto3";
package user;

import "base.proto";

message RegisterRequest {
  string account = 1;
  string password = 2;
}

message RegisterReply {
  base.Reply reply = 1;
}

service Account {
  rpc Register(RegisterRequest) returns (RegisterReply);
}